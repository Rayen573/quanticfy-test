# Example environment configuration
# Copy to .env and fill in values

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=quanticfy
DB_PASSWORD=changeme
DB_NAME=quanticfy_test

# When true, the app skips connecting to the database (useful if DB isn't ready)
SKIP_DB=true